import bcrypt from "bcryptjs";
import { insert, readDb, writeDb } from "./json-db";
import type { User } from "../types";
export async function seedDatabase() { const users = await readDb<User>("users"); if (users.length) return; const now = new Date().toISOString(); const accounts = [["superadmin@salarymanager.com", "Admin@123", "Super Admin"], ["admin@salarymanager.com", "Admin@123", "Admin"], ["advisor@salarymanager.com", "Advisor@123", "Finance Advisor"], ["auditor@salarymanager.com", "Auditor@123", "Auditor"], ["user@salarymanager.com", "User@123", "Normal User"]] as const; for (const [email, password, role] of accounts) await insert<User>("users", { email, fullName: role, mobile: "9999999999", salaryAmount: 104500, salaryDate: 1, passwordHash: await bcrypt.hash(password, 12), role, status: "active", failedLoginAttempts: 0, loginHistory: [], createdAt: now, updatedAt: now }); await writeDb("roles", [{ id: "super-admin", name: "Super Admin", permissions: ["*"] }, { id: "admin", name: "Admin", permissions: ["users.read", "reports.read", "categories.manage"] }, { id: "finance-advisor", name: "Finance Advisor", permissions: ["financial.read", "recommendations.manage"] }, { id: "auditor", name: "Auditor", permissions: ["audit.read", "reports.read"] }, { id: "user", name: "Normal User", permissions: ["financial.manage", "reports.read"] }]); await writeDb("categories", ["Rent", "Food", "Groceries", "Electricity", "Travel", "Shopping", "Medical", "Investment", "Other"].map((name, i) => ({ id: `category-${i + 1}`, name, type: "expense" }))); }
